#we can use simple string functions
#to get or change properties of the strings
#like len() can be used to get lenght of the string
#where argument is the string itself
#other functions
#lower() changes the string to lower case
#upper() changes the string to upper case
#these 2 functions will act on the strings unlike the above
#where we use a dot notation to act on a string

#for example
#rap = "WhY tHis KoLla VEri KolLa  vERi KollA veRRI dI "
#print(rap.lower())
#which prints the whole thing in lower case

#str() function turns non-strings into strings!
#the difference bt the 2 catagories are
#lower() & upper() acts on strings only
#but the other 2 works with any other datatype

#get the name of the user and
#print the len the user name and 
#print the line
#Howdy user(in caps),so length of your
#name is namelength(make this number as string & print)

